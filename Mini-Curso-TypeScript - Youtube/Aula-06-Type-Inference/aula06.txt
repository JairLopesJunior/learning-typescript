// Type Inference

Quando definir a variavel ja iniciando com um valor, não precisa typar por que o TypeScript ja fez isto.

let message = "Mensagem";

window.addEventListener("click", (e) => {
  console.log(e.target);
});

O evento do metodo acima esta tipado como MouseEvent, porem não é necessário colocar.
