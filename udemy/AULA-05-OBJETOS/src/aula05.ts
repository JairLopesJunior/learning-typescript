let aluno: {nome: string, aprovado: boolean, idade: number, notas: number[]};

// Campos opcionais em um objeto Ex: { aprovado?: boolean }

aluno = {
    nome: "Jair",
    aprovado: true,
    idade: 28,
    notas: [1, 2, 3, 7]
}